# Integration Gateway - Environment Variables

# =============================================================================
# Configuração Básica
# =============================================================================

# Porta do servidor
PORT=8081

# Ambiente (development, sandbox, production)
ENVIRONMENT=development

# Diretórios
CONFIG_DIR=./config/connectors
CERTS_DIR=./certs

# =============================================================================
# Secrets (API Keys, OAuth Secrets, etc)
# =============================================================================

# Formato: SECRET_{REF_UPPER}
# Onde REF é a referência no YAML (com - substituído por _)

# Exemplo 1: API Key para ViaCEP
# YAML: api_key.key_ref = "viacep-api-key"
# SECRET_VIACEP_API_KEY=sua-chave-aqui

# Exemplo 2: OAuth2 Client Secret
# YAML: oauth2.client_secret_ref = "oauth-client-secret"
# SECRET_OAUTH_CLIENT_SECRET=seu-secret-aqui

# Exemplo 3: Receita Federal (mTLS não precisa de secret em env, usa certificado)

# =============================================================================
# Certificados ICP-Brasil (para mTLS)
# =============================================================================

# Diretório de certificados (já configurado acima)
# CERTS_DIR=./certs

# Path do CA certificate da ICP-Brasil (opcional)
# Se não definido, usa system CA pool
# ICP_CA_CERT_PATH=/path/to/icp-brasil-ca.pem

# Para desenvolvimento/sandbox apenas: skip TLS verification (NÃO usar em produção!)
# MTLS_SKIP_VERIFY=false

# =============================================================================
# Observabilidade (futuro)
# =============================================================================

# Log level (debug, info, warn, error)
LOG_LEVEL=info

# Prometheus metrics
# METRICS_ENABLED=true
# METRICS_PORT=9090

# Distributed tracing
# TRACING_ENABLED=true
# JAEGER_ENDPOINT=http://jaeger:14268/api/traces

# =============================================================================
# Cache (Redis - futuro)
# =============================================================================

# REDIS_URL=redis://localhost:6379
# REDIS_PASSWORD=
# REDIS_DB=0

# =============================================================================
# Database (PostgreSQL - para Certificate Manager)
# =============================================================================

# DATABASE_URL=postgresql://user:password@localhost:5432/bgc_integrations?sslmode=disable

# =============================================================================
# Exemplos de Secrets para Conectores
# =============================================================================

# ViaCEP (API pública, sem auth necessária)
# N/A

# Receita Federal (mTLS)
# Certificado em: certs/icp-brasil-receita-prod.pem
# Chave em: certs/icp-brasil-receita-prod.key
# N/A (não precisa env var)

# Exemplo fictício: API com OAuth2
# SECRET_EXEMPLO_OAUTH_SECRET=client_secret_123456

# Exemplo fictício: API com API Key
# SECRET_EXEMPLO_API_KEY=api_key_abcdef123456
