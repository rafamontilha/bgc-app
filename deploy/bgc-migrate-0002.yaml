apiVersion: batch/v1
kind: Job
metadata:
  name: bgc-migrate-0002
  namespace: data
spec:
  backoffLimit: 0
  template:
    spec:
      restartPolicy: Never
      volumes:
        - name: migrations
          configMap:
            name: bgc-migrate-0002
      containers:
        - name: psql
          image: postgres:16
          env:
            - name: PGPASSWORD
              valueFrom:
                secretKeyRef:
                  name: pg-postgresql
                  key: postgres-password
          command: ["psql"]
          args:
            - -h
            - pg-postgresql
            - -U
            - postgres
            - -d
            - postgres
            - -p
            - "5432"
            - -v
            - ON_ERROR_STOP=1
            - -f
            - /sql/migrate.sql
          volumeMounts:
            - name: migrations
              mountPath: /sql
              readOnly: true
